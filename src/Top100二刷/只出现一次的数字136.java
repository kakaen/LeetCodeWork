package Top100二刷;

public class 只出现一次的数字136 {
    public int singleNumber(int[] nums){
        //一个元素只出现一次，其他元素出现两次
        //n一定是奇数
        int n=nums.length;
         //设x的下标为i,如果i为奇数
        return 0;
    }
}
