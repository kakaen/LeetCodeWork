package 动态规划算法.最长递增子序列;

import java.util.List;

public class 单词拆分139 {
    public boolean wordBreak(String s, List<String> wordDict) {
        int n=s.length(); //记录字符串的长度
        boolean[] dp=new boolean[n];
        int preIndex=-1; //记录前
        //dp[i]表示前i个字符是否可以被拼接出
        for (int i=0;i<n;i++){
            //前i个元素的值
        }
        return true;
    }
}
